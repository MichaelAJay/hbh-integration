export type ACCOUNT_REF = 'ADMIN' | 'H4H' | 'INVALID_TEST';

/**
 * IMPORTANT
 * Must manually update the following to match ACCOUNT_REF values
 * nutshell-products.body-dto.ts
 */

/**
 * INVALID_TEST is for testing invalid REFs
 */
