export enum EventNotificationPayloadEntityType {
  ORDER = 'Order',
  MENU = 'Menu',
}

/**
 * Implementation note:
 * MENU is here for the sake of completeness.  The system shouldn't be configured to handle Menu type event notifications
 */
