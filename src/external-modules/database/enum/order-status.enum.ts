export enum DbOrderStatus {
  ACCEPTED = 'ACCEPTED',
  CANCELLED = 'CANCELLED',
  ARCHIVED = 'ARCHIVED',
  accepted = 'accepted',
  cancelled = 'cancelled',
  archived = 'archived',
}

/**
 * Implementation note:
 * I prefer canceled over cancelled, but I'm using EZManage value
 */
